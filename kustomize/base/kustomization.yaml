apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app: flask-app
  managed-by: kustomize

namespace: flask-demo-kustomize
